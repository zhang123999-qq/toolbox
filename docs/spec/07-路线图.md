# 首发路线图

> 分批策略：P0 先上高频纯前端，P1 补日常需求，P2 补能力上限，P3 差异化
> 落地点：全部进入 `apps/web/src/tools/<slug>/`
>
> **工具全量清单：** [`../catalog/README.md`](../catalog/README.md) + [`../tools/`](../tools/) 20 份分域明细
>
> **实测分批数量：** P0 148 / P1 322 / P2 332 / P3 68（合计 870）
>
> **网页结构：** [`06-网页结构与信息架构.md`](06-网页结构与信息架构.md) —— 4 大组 + 20 域架构、6 种模板、三层搜索
>
> **开工前置：** [`08-待决事项.md`](08-待决事项.md) —— 8 项待拍板决策

---

## 阶段 0 · 架构骨架（开工第一步）

在写任何工具之前，先把地基搭好：

| 任务 | 产物 |
|---|---|
| 初始化 pnpm workspace + Turborepo | `pnpm-workspace.yaml`、`turbo.json` |
| 建 `apps/web`（Vite + React 19 + TS） | `vite.config.ts` |
| 建 `packages/config`（eslint/tsconfig/tailwind 共享） | 共享配置包 |
| 建 `packages/catalog` + `apps/web/src/catalog` | `categories.ts`、`groups.ts`、`schema.ts`、`loader.ts` |
| 实现路由生成 | `router.tsx` 由 `routes.ts` 生成 |
| **实现 4 大组 + 20 域三层导航** | `GroupNav`、`CategoryGrid`、`SubCategoryFilter` |
| **实现 6 种工具页模板骨架** | `templates/T1`–`T6` |
| **实现三层搜索** | `SearchHero`（首页）+ `SearchDialog`（Cmd+K）+ 工具内搜索 |
| **实现搜索索引生成** | `search-index.ts` + Orama（含拼音/别名/模糊） |
| **实现响应式骨架** | 移动端底部 TabBar + 抽屉 + 工具页双态切换 |
| 建 `packages/ui`（shadcn/ui 封装） | Header、Sidebar、Card、Tabs |
| 写 `scripts/generate-catalog.ts` | 扫描 `tools/*/meta.ts` |
| 写 `scripts/check-tools.ts` | 元数据完整性 + 重复检测 + **模板/大组归属校验** |
| 写 `scripts/generate-sitemap.ts` | 生成 sitemap.xml |
| 配 CI | `ci.yml`：lint + typecheck + check:tools + test + build |

**验收标准：** 能注册 1 个示例工具（json-formatter），走 T2 模板渲染；4 大组导航可用；Cmd+K 搜得到；移动端布局正常；CI 全绿。

---

## 阶段 1 · P0 批次（148 个，纯前端高频）

**目标：** 先做出「能用的工具站」，验证架构与体验

**完整 P0 清单：** 见 [`../tools/*.md`](../tools/) 中「优先级 = P0」的行，共 148 条。

分布最密的几个域（实测）：

| 域 | P0 数量 | 域内占比 |
|---|---:|---:|
| 1 文本与内容 | 28 | 40% |
| 4 开发运维 | 21 | 23% |
| 3 数据格式 | 20 | 33% |
| 2 编码加密安全 | 16 | 27% |
| 6 数学金融 | 15 | 25% |
| 7 随机生成设计 | 11 | 22% |
| 8 图片图形 | 11 | 18% |
| 9 PDF-Office | 9 | 15% |
| 5 时间日期 | 8 | 27% |
| 12 网络SEO | 8 | 16% |
| 16 自动化API测试 | 1 | 3% |

> **注意：** 域 10/11/13/14/15/17/18/19/20 **无 P0**，全部从 P1 起步。
> 这意味着首发版本不覆盖音视频、AI、可视化、Web3、无障碍、扩展、游戏、边缘、教育九大域。

---

## 阶段 2 · P1 批次（WASM / 中等约 100 个）

**目标：** 补齐能力上限（图片 / 音视频 / Office 全链路）

| 工具 | 可行性 | 依赖 |
|---|---|---|
| 图片背景移除 | B | wasm-vips / ONNX |
| 音视频转码 | B | `ffmpeg.wasm` + `media.worker` |
| GIF 制作 | B | `ffmpeg.wasm` |
| PDF OCR | B | `tesseract.js` + `pdfjs` |
| PDF 压缩 | B | `pdf-lib` + `pdfjs` |
| Word / Excel / PPT 转换 | B | `@neo-office/renderer` |
| 语音识别 | B | `whisper.cpp` WASM |
| 本地 LLM | B | `wllama` + WebGPU |
| RAG | B | `wllama` + 向量库 |
| 地图可视化 | A | Leaflet |
| 关系图 | A | AntV G6 |
| WebSocket 测试 | C | 原生 WebSocket |
| 负载测试 | C | Worker + fetch |
| 数据库 ER 图 | A | Mermaid / 自绘 |

---

## 阶段 3 · P2 批次（API / 高级约 100 个）

**目标：** 网络能力扩展，**全部标注「需用户自备 API/Key」**

均满足 `meta.api = true`，且 UI 必须显示数据流向提示。

| 工具 | 说明 |
|---|---|
| SEO 审计 | 用户提供目标 URL 与 Key |
| 网站速度 | 同上 |
| DNS / Whois | 第三方 API |
| SSL 检测 | 第三方 API |
| 短链 | 用户自备短链服务 |
| 翻译 | 用户自备翻译 API |
| 图像生成 | 用户自备图像 API |
| 链上查询 | 用户配置 RPC |
| 天气 | 第三方 API |
| 汇率 | 第三方 API |

---

## 阶段 4 · PWA 与体验收尾

- Workbox 离线缓存策略（工具页可离线打开）
- IndexedDB：收藏、历史
- Manifest + 安装提示
- Lighthouse 门禁：性能 / SEO / 无障碍

---

## 开工前置条件（需拍板）

> **全部 8 项待拍板决策已集中至 [`08-待决事项.md`](08-待决事项.md)**，含背景、选项与建议。
> 开工前请逐项确认，本文不再重复展开。摘要：

| # | 事项 | 阻塞阶段 |
|---:|---|---|
| 1 | **框架二选一**：Next.js 15 vs Vite | 阶段 0 |
| 2 | **4 大组 ↔ 20 域口径**（3 个子组无对应域） | 阶段 0 |
| 3 | **工具粒度**：独立路由 vs 多 Tab 合并 | 阶段 0 |
| 4 | **870 个工具的 `template` 字段回填** | 阶段 1 |
| 5 | **WASM 分发**：自建 CDN vs 公共 CDN | 阶段 2 |
| 6 | **i18n 范围**：中文单语 vs 中英双语 | 阶段 0 |
| 7 | **D 类工具数据流向提示样式** | 阶段 2 |
| 8 | **「21 → 20」合并项说明** | 阶段 0 |

---

## 进度追踪

- [x] 阶段 0：架构骨架（catalog + 路由 + 搜索 + 布局 + CI）—— **2026-09-23 完成**
- [ ] 阶段 1：P0 批次
- [ ] 阶段 2：P1 批次
- [ ] 阶段 3：P2 批次
- [ ] 阶段 4：PWA 离线 + 收藏 + 历史

**当前状态：阶段 0 骨架已完成，阶段 1（P0 148 个）未开始。**

### 阶段 0 交付内容（2026-09-23）

| 项 | 产物 | 状态 |
|---|---|---|
| pnpm workspace + Turborepo | `pnpm-workspace.yaml` / `turbo.json` | ✅ |
| `packages/catalog` | 20 域真源表、4 大组、Zod 契约、路由生成、搜索索引 | ✅ |
| `packages/search` | 检索门面（Orama 适配位预留） | ✅ |
| `apps/web` | Vite + React 19 + TS + Tailwind v4，路由由 catalog 生成 | ✅ |
| 示例工具 | `json-formatter`（#131 / T2 / 8 文件齐全 / 8 个单测通过） | ✅ |
| 脚本 | `generate-catalog` / `check-tools` / `generate-sitemap` | ✅ |
| CI | `.github/workflows/ci.yml` | ✅ |
| 门禁 | typecheck 0 error / test 8 passed / build 成功 | ✅ |

> ⚠️ **首屏 JS 未达预算**：实测 262KB（gzip 84KB），目标 < 50KB。
> 根因是 React 19 + React Router 的基线体积，与技术栈定稿冲突，
> 详见 [`DEVELOPMENT.md`](../DEVELOPMENT.md) §17。
