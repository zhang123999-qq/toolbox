# 依赖与构建产物
node_modules
dist
dist-ssr
.turbo
coverage
*.tsbuildinfo

# 报告与本地临时产物
playwright-report
test-results
blob-report
tmp-shots
dist-release

# 生成物：由脚本产出，不参与格式化（避免与生成器来回抖动）
packages/catalog/src/tools.generated.ts
apps/web/public/sitemap.xml
apps/web/public/robots.txt

# 锁文件：由 pnpm 维护，格式化会破坏其结构
pnpm-lock.yaml

# 二进制与图片
*.png
*.jpg
*.jpeg
*.ico
*.woff
*.woff2
*.tar.gz

# 铺量期的临时生成脚本（一次性，用完即删）
tmp/

# 容器化全量测试的过程日志（.agent/test-logs）：是输出不是源码，不该被格式化
.agent/test-logs
