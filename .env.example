# ─────────────────────────────────────────────────────────────────────────────
# 环境变量样例（复制到 .env 后填写）
#
# 约束条款：docs/DEVELOPMENT.md §8.5「外部 API 配置（硬约束）」
#   1. 任何需要调用外部 API 的工具或服务，都必须在本文件登记其配置项
#   2. 每个配置项必须写清：变量名 / 用途 / 是否必填 / 获取途径 / 填写格式 / 示例值
#   3. 服务名统一「中英混合」写法，例如 OpenAI（开放AI大模型服务）
#   4. 严禁把真实密钥写进本文件或提交任何 .env；值一律留空或填占位符
#
# 机检：pnpm check:env（校验 .env 已忽略、工具已登记、无疑似真实密钥）
#
# ⚠️ 关于 VITE_ 前缀：Vite 会把 VITE_ 开头的变量**内联进浏览器产物**，任何人查看
#    dist/*.js 都能读到。因此密钥类变量**一律不得**使用 VITE_ 前缀。
#    本文件里只有 VITE_TOOLBOX_AI_API_BASE / VITE_TOOLBOX_AI_MODEL 允许带前缀
#    （它们不是密钥，用途是给自部署场景预置默认端点与模型）。
# ─────────────────────────────────────────────────────────────────────────────

# ═════════════════════════════════════════════════════════════════════════════
# 一、变量字典（逐项说明：用途 / 必填 / 获取途径 / 格式 / 示例）
# ═════════════════════════════════════════════════════════════════════════════

# ── 站点与构建（构建期脚本读取，不进浏览器产物）────────────────────────────────
# 变量：SITE_ORIGIN
# 用途：sitemap.xml / robots.txt / canonical 使用的站点绝对地址
# 必填：否（缺省取 packages/catalog/src/site.ts 的 SITE_ORIGIN）
# 获取：无需申请，填你自己的域名
# 格式：https://<域名>，末尾不要带斜杠
# 示例：SITE_ORIGIN=https://pre.006336.xyz
SITE_ORIGIN=

# 变量：COREPACK_NPM_REGISTRY
# 用途：Docker 构建时 corepack 下载 pnpm 的镜像源（默认已指向 npmmirror）
# 必填：否（国内网络建议保留默认值）
# 获取：无需申请；公共镜像 https://registry.npmmirror.com
# 格式：https://<镜像域名>
# 示例：COREPACK_NPM_REGISTRY=https://registry.npmmirror.com
COREPACK_NPM_REGISTRY=

# ── 部署与发布（deploy/binary/install.sh、toolboxctl、gh 读取）─────────────────
# 变量：GITHUB_TOKEN
# 用途：GitHub（代码托管平台）访问令牌；上传 Release 附件、从私有仓库下载资产
# 必填：否（仓库已公开则匿名即可；上传附件与私有仓库必需）
# 获取：GitHub（代码托管平台）→ Settings → Developer settings →
#       Personal access tokens → Fine-grained tokens
#       https://github.com/settings/personal-access-tokens
# 格式：github_pat_…（细粒度）或 ghp_…（经典）；只需 Contents / Releases 读写权限
# 示例：GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxxxx
# ⚠️ 严禁提交真实值；本文件保持为空
GITHUB_TOKEN=

# 变量：TOOLBOX_REPO
# 用途：覆盖一键安装脚本取包的仓库（默认 zhang123999-qq/toolbox）
# 必填：否
# 获取：无需申请，填 <owner>/<repo>
# 格式：<owner>/<repo>
# 示例：TOOLBOX_REPO=zhang123999-qq/toolbox
TOOLBOX_REPO=

# 变量：TOOLBOX_PROXY
# 用途：一键安装脚本下载产物时使用的 HTTP 代理
# 必填：否（目标机能直连 GitHub 就不用填）
# 获取：无需申请，填你自己的代理地址
# 格式：http://<host>:<port>
# 示例：TOOLBOX_PROXY=http://127.0.0.1:10808
TOOLBOX_PROXY=

# ── AI 大模型服务（D 类工具：页面填写优先，下列为自部署预置项）────────────────
# 变量：VITE_TOOLBOX_AI_API_BASE
# 用途：OpenAI（开放AI大模型服务）兼容的 chat completions 端点；
#       自部署时可预置，用户就不必手填接口地址
# 必填：否（留空则由用户在页面填写）
# 获取：OpenAI（开放AI大模型服务）https://platform.openai.com/docs/api
#       DeepSeek（深度求索大模型服务）https://platform.deepseek.com/docs
#       通义千问（阿里云大模型服务）https://dashscope.console.aliyun.com/
#       智谱 GLM（智谱AI大模型服务）https://open.bigmodel.cn/dev/howuse/model
#       Moonshot Kimi（月之暗面大模型服务）https://platform.moonshot.cn/docs
#       Ollama（本地大模型运行时）http://localhost:11434/v1
# 格式：https://<域名>/v1（脚本会去掉末尾斜杠后拼 /chat/completions）
# 示例：VITE_TOOLBOX_AI_API_BASE=https://api.openai.com/v1
# 说明：非密钥，允许 VITE_ 前缀
VITE_TOOLBOX_AI_API_BASE=

# 变量：VITE_TOOLBOX_AI_MODEL
# 用途：默认模型名；自部署时预置
# 必填：否
# 获取：同上各家控制台的「模型列表」
# 格式：厂商模型标识字符串
# 示例：VITE_TOOLBOX_AI_MODEL=gpt-4o-mini
# 说明：非密钥，允许 VITE_ 前缀
VITE_TOOLBOX_AI_MODEL=

# 变量：TOOLBOX_AI_API_KEY
# 用途：供「自建代理」在服务端注入的密钥；前端代码**不读取**此变量
# 必填：否（绝大多数场景由用户在页面填写，密钥不出浏览器）
# 获取：见上面各家控制台的 API Keys 页面
# 格式：sk-…（各家前缀不同，原样粘贴）
# 示例：TOOLBOX_AI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
# ⚠️ 禁止改名为 VITE_ 前缀，否则会被打进浏览器产物
TOOLBOX_AI_API_KEY=

# ── 测试（Playwright E2E）────────────────────────────────────────────────────
# 变量：E2E_BASE_URL
# 用途：E2E 用例访问的地址（不设则用 playwright.config.ts 的 4173 端口）
# 必填：否
# 获取：无需申请
# 格式：http://<host>:<port>
# 示例：E2E_BASE_URL=http://127.0.0.1:4173
E2E_BASE_URL=

# 变量：E2E_NO_WEBSERVER
# 用途：设为 1 时复用已启动的预览服务，不自动拉起 vite preview
# 必填：否
# 获取：无需申请
# 格式：1 / 0
# 示例：E2E_NO_WEBSERVER=1
E2E_NO_WEBSERVER=

# ═════════════════════════════════════════════════════════════════════════════
# 二、按工具逐项列出（工具 → 所需配置项）
# ═════════════════════════════════════════════════════════════════════════════
# 这 5 个是可行性 D 类工具（需调用外部大模型接口），共用同一套 OpenAI 兼容配置。
# 变量含义见上面「AI 大模型服务」一节；工具页本身会在页面上明示数据流向。
#
# 工具：summarize（文本摘要）
#   需要：VITE_TOOLBOX_AI_API_BASE / VITE_TOOLBOX_AI_MODEL / TOOLBOX_AI_API_KEY
#   说明：extractive（提取式）模式为纯本地计算，不需要任何配置；
#         只有 abstractive（生成式）模式才需要接口
#
# 工具：rewrite（文本改写）
#   需要：VITE_TOOLBOX_AI_API_BASE / VITE_TOOLBOX_AI_MODEL / TOOLBOX_AI_API_KEY
#   说明：全部模式都需调用接口
#
# 工具：translate（文本翻译）
#   需要：VITE_TOOLBOX_AI_API_BASE / VITE_TOOLBOX_AI_MODEL / TOOLBOX_AI_API_KEY
#   说明：全部模式都需调用接口
#
# 工具：title-gen（标题生成）
#   需要：VITE_TOOLBOX_AI_API_BASE / VITE_TOOLBOX_AI_MODEL / TOOLBOX_AI_API_KEY
#   说明：全部模式都需调用接口
#
# 工具：tag-gen（标签生成）
#   需要：VITE_TOOLBOX_AI_API_BASE / VITE_TOOLBOX_AI_MODEL / TOOLBOX_AI_API_KEY
#   说明：全部模式都需调用接口
#
# 服务：deploy/binary/install.sh（一键安装脚本）
#   需要：GITHUB_TOKEN / TOOLBOX_REPO / TOOLBOX_PROXY
#   说明：仓库公开时匿名可装；私有仓库或内网机器按需填写
#
# 服务：scripts/generate-sitemap.ts（sitemap 与 robots 生成）
#   需要：SITE_ORIGIN
#   说明：预发 / 临时环境覆盖正式域名时使用
#
# 服务：deploy/docker/Dockerfile（容器构建）
#   需要：COREPACK_NPM_REGISTRY / SITE_ORIGIN
#   说明：以 --build-arg 传入，也可由同名环境变量提供

# ═════════════════════════════════════════════════════════════════════════════
# 三、新增工具或服务时怎么补
# ═════════════════════════════════════════════════════════════════════════════
#   # ── 变量：<NAME> ──────────────────────────────────
#   # 用途：<一句话>
#   # 必填：是 / 否（否 的话说明缺省行为）
#   # 获取：<服务名（中文说明）> + 控制台地址
#   # 格式：<取值形态>
#   # 示例：<NAME>=<占位值>
#   <NAME>=
# 然后在「二、按工具逐项列出」加一段「工具：<slug>（中文名）」。
# 服务名务必写成 OpenAI（开放AI大模型服务）这种中英混合形式。
