# 文本改写

同义改写、润色。

## 用途

把一段话换个说法：润色表达、转正式书面语、改口语、压短或展开写——适合改文案、改邮件、改周报。

## 输入

| 字段      | 类型   | 约束                                   |
| --------- | ------ | -------------------------------------- |
| `text`    | string | 最大 20,000 字符                       |
| `apiBase` | string | 接口地址（Base URL），最大 20,000 字符 |
| `apiKey`  | string | API Key，最大 20,000 字符              |
| `model`   | string | 模型名，最大 20,000 字符               |

待改写的文本 + 接口信息（接口地址、API Key、模型名）。

## 输出

| 字段   | 类型   | 说明                 |
| ------ | ------ | -------------------- |
| `text` | string | 模型返回的改写文本。 |

## 选项

| 选项    | 取值                                                                                          | 默认     | 说明     |
| ------- | --------------------------------------------------------------------------------------------- | -------- | -------- |
| `style` | `polish`（润色） / `formal`（正式） / `casual`（口语） / `concise`（压缩） / `expand`（展开） | `polish` | 改写诉求 |

## 限制

- **需要自备 OpenAI 兼容接口**，本工具不内置任何模型与额度
- 改写结果由模型生成，可能改变事实细节或语气，重要文本请人工复核
- 不提供「只改同义词、不动句式」的严格改写：模型的改写粒度不可控
- 不保留 Markdown 结构保证：带格式的文本可能被模型改写丢结构
- 输入上限 20,000 字符

## 数据流向

**会向外部接口发送数据。** 本工具需要你自备大模型接口（OpenAI 兼容的 `/chat/completions`）：

- 请求由**浏览器直接**发往你填写的接口地址，**不经本项目任何服务器**
- 发送内容：待处理的正文 + 一段指令（prompt），以及你填的模型名
- 凭据：API Key 放在 `Authorization` 请求头里，只存在于当前页面的输入框中，
  **不写入 localStorage**，也不会随页面分享带走
- 超时 60 秒；返回结构按 OpenAI 的 `choices[0].message.content` 解析

`meta.api = true`。没填接口信息就点运行时，工具会给出缺项提示而不会发请求。

## 示例

输入：

```text
这个东西挺好的，就是有点小问题，你看看能不能改一下。
```

输出：

```text
（由你接入的模型返回，此处不预设内容）
```

## 元信息

| 项       | 值                      |
| -------- | ----------------------- |
| 全局编号 | #47                     |
| 域       | `text`（文本与内容）    |
| 大组     | `dev`                   |
| 优先级   | P2                      |
| 可行性   | D（用户自备 API / Key） |
| 模板     | T2（双栏）              |
