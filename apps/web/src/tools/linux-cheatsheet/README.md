# Linux 命令

按分类列出 50+ 条常用 Linux 命令、说明与示例。

## 输入

| 字段   | 类型   | 约束              |
| ------ | ------ | ----------------- |
| `text` | string | 最大 200,000 字符 |

输入框只作触发用。

## 选项

| 选项 key   | 界面标签 | 取值：all / file / process / network / permission / disk / user / search / archive |
| ---------- | -------- | ---------------------------------------------------------------------------------- |
| `category` | 分类     |                                                                                    |

## 输出

`命令 | 说明 | 示例` 三列表，每个分类一个二级标题。

## 元信息

| 项       | 值                               |
| -------- | -------------------------------- |
| 全局编号 | #228                             |
| 域       | `devops`（开发 / 运维 / 云原生） |
| 大组     | `dev`                            |
| 优先级   | P0                               |
| 可行性   | A（纯 JS）                       |
| 模板     | T2（双栏）                       |
