# 标题生成

根据内容生成标题。

## 用途

给一段正文配标题：可选平实、SEO、疑问句、「如何……」四种风格，一次给多个候选，挑一个用。

## 输入

| 字段      | 类型   | 约束                                   |
| --------- | ------ | -------------------------------------- |
| `text`    | string | 最大 20,000 字符                       |
| `apiBase` | string | 接口地址（Base URL），最大 20,000 字符 |
| `apiKey`  | string | API Key，最大 20,000 字符              |
| `model`   | string | 模型名，最大 20,000 字符               |

正文内容 + 接口信息（接口地址、API Key、模型名）。

## 输出

| 字段   | 类型   | 说明                           |
| ------ | ------ | ------------------------------ |
| `text` | string | 模型返回的标题候选，每行一个。 |

## 选项

| 选项    | 取值                                                                             | 默认      | 说明     |
| ------- | -------------------------------------------------------------------------------- | --------- | -------- |
| `style` | `neutral`（平实） / `seo`（利于搜索） / `question`（疑问式） / `howto`（实操式） | `neutral` | 标题风格 |
| `count` | `1` / `3` / `5`                                                                  | `3`       | 候选数量 |

## 限制

- **需要自备 OpenAI 兼容接口**，本工具不内置任何模型与额度
- 候选数量只是写进指令的要求，模型不一定严格按数量返回
- 不检测标题长度与关键词密度，SEO 效果请自行判断
- 同一段正文多次生成可能得到不同结果
- 输入上限 20,000 字符

## 数据流向

**会向外部接口发送数据。** 本工具需要你自备大模型接口（OpenAI 兼容的 `/chat/completions`）：

- 请求由**浏览器直接**发往你填写的接口地址，**不经本项目任何服务器**
- 发送内容：待处理的正文 + 一段指令（prompt），以及你填的模型名
- 凭据：API Key 放在 `Authorization` 请求头里，只存在于当前页面的输入框中，
  **不写入 localStorage**，也不会随页面分享带走
- 超时 60 秒；返回结构按 OpenAI 的 `choices[0].message.content` 解析

`meta.api = true`。没填接口信息就点运行时，工具会给出缺项提示而不会发请求。

## 示例

输入：

```text
本文记录了把静态站点部署到服务器的过程：先在本地构建产物，再用 Nginx 托管静态文件，最后用 Docker 复现线上环境做一致性校验。
```

输出：

```text
（由你接入的模型返回，此处不预设内容）
```

## 元信息

| 项       | 值                      |
| -------- | ----------------------- |
| 全局编号 | #49                     |
| 域       | `text`（文本与内容）    |
| 大组     | `dev`                   |
| 优先级   | P2                      |
| 可行性   | D（用户自备 API / Key） |
| 模板     | T2（双栏）              |
